import { expect } from 'chai'

export function runTests() {
    describe('DiffNotifier', () => {
        it('should allow subscriptions', () => {
            expect(true).to.be.true
        })
    })
}
